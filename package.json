{
  "name": "sync.js",
  "version": "0.1.0",
  "description": "App with web interface to sync files to another computer with external hdd",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.14.1",
    "express": "^4.13.3"
  },
  "devDependencies": {
    "grunt": "^0.4.5"
  }
}
